# -*- mode: snippet -*-
# name: twbatch
# key: twbatch
# --
#+begin_src sql :db-conn dsm-${1:dev}
  select task_status, count(*)
  from data_service_manager.tasks
  where batch_id = '$2'
  group by 1;
#+end_src
